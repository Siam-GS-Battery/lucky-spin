/** Placeholder — real UI (wheel, picker, reveal, admin) is ported from index.html in task 7. */
export function BoothPage() {
  return (
    <main className="grid min-h-full place-items-center font-display text-white">
      <h1>BoothPage</h1>
    </main>
  );
}
