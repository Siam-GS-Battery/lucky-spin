/** Placeholder — real UI is ported from login.html in task 7. */
export function LoginPage() {
  return (
    <main className="grid min-h-full place-items-center font-display text-white">
      <h1>LoginPage</h1>
    </main>
  );
}
